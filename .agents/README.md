# Repo Agent Infrastructure

Root instructions live in `../AGENTS.md`.

- `rules/` holds repo-local operating notes.
- `commands/` is reserved for repeatable command recipes.
- `skills/` is reserved for repo-local skills when a repeated fragile workflow justifies one.
